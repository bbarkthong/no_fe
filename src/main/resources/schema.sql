CREATE TABLE BOARD 
(
    ID   INTEGER ,
    TITLE VARCHAR(255),
    PRIMARY KEY (ID)
);

CREATE TABLE POST 
(
    ID   INTEGER ,
    TITLE VARCHAR(255),
    CONTENT VARCHAR(255)
    POST_ID INTEGER ,
    PRIMARY KEY (ID)
);

